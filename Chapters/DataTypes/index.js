// Number
let age = 20;

// String
let name = "João Gabriel"

// Object
let fullName = {
    first: "João",
    last: "Melão"
};

// Booleans
let truth = true;
let ty = false;

// Arrays
let brands = ["AMD", "Intel", "Nvidia"];

brands.forEach(brandName => {
    console.log(brandName);
});

// Null
let empty = null;

// Undefined
let random;